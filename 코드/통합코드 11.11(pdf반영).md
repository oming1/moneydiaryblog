# =================================================================
# Hugo 마크다운 파일 완전 자동화 (신규 파일만 처리 모드 + PDF GitHub 경로 설정)
# =================================================================
#
# 기능:
# 1. 누락된 _index.md 파일 자동 생성 (weight 포함)
# 2. 기존 파일은 건드리지 않음 (weight, lastmod 보존)
# 3. SEO 메타 태그 추가
# 4. [[target|alias]] 또는 [[target]] 형태의 위키링크를 표준 Markdown 링크로 변환
# 5. [[image.png]] 형태의 위키링크를 ![Alt Text](/images/image.png) 형태로 변환
# 6. ![](Pasted image.png) 형태를 ![](/images/Pasted image.png) 형태로 변환
# 7. 프론트 매터에 sidebar: open: false 설정 반영
# 8. PDF 파일 경로를 GitHub 저장소 경로로 변환
#
# =================================================================
# 경고: 이 스크립트는 파일 내용을 직접 수정합니다.
# 반드시 작업 전에 'content' 폴더 전체를 백업해두세요!
# =================================================================

# --- 사용자 설정 시작 ---
# =================================================================

# ⚠️ 중요: 처리 모드 선택
# $false = 신규 파일만 처리 (기본값, 일상적 사용 - 기존 파일의 weight, lastmod 보존)
# $true  = 전체 재정리 (폴더 구조 변경 시에만 사용 - 모든 파일의 weight, type 등 재계산)
$fullRebuildMode = $false

# 1. 프로젝트의 content 폴더 경로 (반드시 실제 경로로 수정)
$contentPath = "D:\GitHub\moneydiaryblog\content"

# 2. 프로젝트의 static 폴더 경로 (반드시 실제 경로로 수정)
$staticPath = "D:\GitHub\moneydiaryblog\static"

# 3. static 폴더 내 이미지 폴더명 (일반적으로 "images")
$imagesStaticSubPath = "images"

# 4. 웹사이트 상에서 이미지가 접근될 기본 경로
$webImagePath = "/images"

# 5. PDF GitHub 저장소 기본 URL
$pdfGitHubBaseUrl = "https://raw.githubusercontent.com/oming1/moneydiary-assets/main/documents"

# 6. 처리할 섹션과 해당 permalinks 매핑
$sectionPermalinks = @{
    "산업-테마공부" = "/industry-study/"
    "기업공부" = "/company-analysis/"
    "Daily시황정리" = "/daily-summary/"
    "earning" = "/earnings/"
    "투자아이디어" = "/investment-ideas/"
}

# 7. 스크립트가 처리할 최상위 섹션 폴더 이름 목록
$sections = @("Daily시황정리", "산업-테마공부", "기업공부", "earning", "투자아이디어")

# =================================================================
# --- 사용자 설정 끝 ---

Write-Host "========================================" -ForegroundColor Cyan
Write-Host "   Hugo 마크다운 완전 자동화 (PDF GitHub 경로 설정)" -ForegroundColor Cyan
if ($fullRebuildMode) {
    Write-Host "   ⚠️  전체 재정리 모드 (모든 파일 처리)" -ForegroundColor Red
} else {
    Write-Host "   ✅ 신규 파일만 처리 모드 (기존 파일 보존)" -ForegroundColor Green
}
Write-Host "========================================" -ForegroundColor Cyan
Write-Host "대상 경로: $contentPath"
Write-Host "정적 파일 경로: $staticPath"
Write-Host "이미지 웹 경로: $webImagePath"
Write-Host "PDF GitHub URL: $pdfGitHubBaseUrl"
Write-Host "처리할 섹션: $($sections -join ', ')"
Write-Host ""
Write-Host "🚨 경고: 이 작업은 파일 내용을 직접 수정합니다. 백업 필수! 🚨" -ForegroundColor Red
Write-Host ""

# URL 안전성을 위한 함수 정의
function Get-SafeSlug {
    param([string]$text)
    
    $slug = $text.ToLower()
    $slug = $slug -replace '[^a-z0-9가-힣\s-_]', ''
    $slug = $slug -replace '\s+', '-'
    $slug = $slug -replace '-+', '-'
    $slug = $slug.Trim('-')
    
    return $slug
}

# weight 결정 함수
function Get-AssignedWeight {
    param(
        [string]$folderPath,
        [string]$contentRootPath,
        [string]$folderName,
        [array]$topLevelSections
    )
    
    $assignedWeight = 9999
    $relativePathFromContent = $folderPath.Replace($contentRootPath, "").TrimStart('\')
    $pathParts = $relativePathFromContent.Split('\')
    $folderDepth = $pathParts.Count
    
    if ($folderDepth -eq 1) {
        $topLevelIndex = [array]::IndexOf($topLevelSections, $folderName)
        if ($topLevelIndex -ne -1) {
            $assignedWeight = $topLevelIndex + 1
        } else {
            $assignedWeight = 1000 + (Get-Random -Minimum 1 -Maximum 100)
        }
    } elseif ($folderName -match '^(\d+)\.') {
        $assignedWeight = [int]$matches[1]
    } else {
        $assignedWeight = 2000 + (Get-Random -Minimum 1 -Maximum 100)
    }
    
    return $assignedWeight
}

# 프론트 매터 존재 여부 확인 함수
function Test-HasFrontMatter {
    param([string]$content)
    return ($content -match '(?s)^---\s*\n([\s\S]*?)\n---\s*\n')
}

# =================================================================
# 1단계: 누락된 _index.md 파일들 자동 생성 및 업데이트
# =================================================================

Write-Host "=== 1단계: 누락된 _index.md 파일 생성 및 업데이트 ===" -ForegroundColor Magenta

foreach ($section in $sections) {
    $sectionPath = Join-Path $contentPath $section
    
    if (Test-Path $sectionPath) {
        Write-Host "섹션 스캔 중: $section" -ForegroundColor Green
        
        $allFolders = @($sectionPath) + (Get-ChildItem -Path $sectionPath -Directory -Recurse | Select-Object -ExpandProperty FullName)
        
        foreach ($folderPath in $allFolders) {
            $indexPath = Join-Path $folderPath "_index.md"
            $folderName = Split-Path $folderPath -Leaf
            $relativePath = $folderPath.Replace($contentPath, "").TrimStart('\')
            
            $assignedWeight = Get-AssignedWeight -folderPath $folderPath -contentRootPath $contentPath -folderName $folderName -topLevelSections $sections
            
            if (-not (Test-Path $indexPath)) {
                $indexContent = @"
---
title: "$folderName"
weight: $assignedWeight
type: docs
sidebar:
  open: false
---

"$folderName" 이 섹션의 내용입니다.
"@
                Set-Content -Path $indexPath -Value $indexContent -Encoding UTF8
                Write-Host "  ✓ 생성됨: /$relativePath/_index.md (Weight: $assignedWeight)" -ForegroundColor Green
            } elseif ($fullRebuildMode) {
                $currentContent = Get-Content -Path $indexPath -Raw -Encoding UTF8
                if ($currentContent -match '(?s)^---\s*\n([\s\S]*?)\n---\s*\n([\s\S]*)$') {
                    $frontMatterBlock = $matches[1]
                    $bodyContent = $matches[2]
                    
                    $newFrontMatterBlock = $frontMatterBlock -replace '(?m)^title:\s*".*?"', "title: `"$folderName`""
                    if ($newFrontMatterBlock -notmatch '(?m)^title:') {
                        $newFrontMatterBlock = "title: `"$folderName`"`n" + $newFrontMatterBlock
                    }
                    
                    $newFrontMatterBlock = $newFrontMatterBlock -replace '(?m)^weight:\s*\d+', "weight: $assignedWeight"
                    if ($newFrontMatterBlock -notmatch '(?m)^weight:') {
                        $newFrontMatterBlock += "`nweight: $assignedWeight"
                    }
                    
                    $newFrontMatterBlock = $newFrontMatterBlock -replace '(?m)^type:\s*.+', "type: docs"
                    if ($newFrontMatterBlock -notmatch '(?m)^type:') {
                        $newFrontMatterBlock += "`ntype: docs"
                    }
                    
                    $newFrontMatterBlock = $newFrontMatterBlock -replace '(?m)^sidebar:\s*\n\s*open:\s*.+', "sidebar:`n  open: false"
                    if ($newFrontMatterBlock -notmatch '(?m)^sidebar:') {
                         $newFrontMatterBlock += "`nsidebar:`n  open: false"
                    }
                    
                    $updatedContent = @"
---
$newFrontMatterBlock
---
$bodyContent
"@
                    Set-Content -Path $indexPath -Value $updatedContent -Encoding UTF8
                    Write-Host "  ✓ 업데이트됨: /$relativePath/_index.md (Weight: $assignedWeight)" -ForegroundColor DarkGreen
                }
            } else {
                Write-Host "  - 건너뜀 (기존 _index.md 파일): /$relativePath/_index.md" -ForegroundColor DarkGray
            }
        }
    }
}

Write-Host ""

# =================================================================
# 2단계: Permalinks를 고려한 URL 매핑 생성 (이미지 파일 포함)
# =================================================================

Write-Host "=== 2단계: Permalinks 기반 URL 매핑 생성 (이미지 포함) ===" -ForegroundColor Magenta

$linkMap = @{}

Write-Host "Markdown 파일 매핑 중..." -ForegroundColor Green

Get-ChildItem -Path $contentPath -Filter "*.md" -Recurse -File | ForEach-Object {
    $filePath = $_.FullName
    $fileName = $_.BaseName
    $relativeContentPath = $filePath.Replace($contentPath, "").Replace("\", "/").TrimStart('/')
    
    $section = ""
    
    foreach ($sectionName in $sections) {
        if ($relativeContentPath.StartsWith($sectionName + "/") -or $relativeContentPath -eq $sectionName) {
            $section = $sectionName
            break
        }
    }
    
    if (-not $section) {
        return
    }
    
    $basePermalink = $sectionPermalinks[$section]
    $hugoUrl = ""
    
    if ($fileName -eq "_index") {
        $folderPath = Split-Path $relativeContentPath -Parent
        if ($folderPath -eq $section) {
            $hugoUrl = $basePermalink
        } else {
            $subPath = $folderPath.Replace($section, "").Trim('/')
            if ($subPath) {
                $subSlugParts = $subPath.Split('/') | ForEach-Object { Get-SafeSlug $_ }
                $hugoUrl = $basePermalink + ($subSlugParts -join '/') + "/"
            } else {
                $hugoUrl = $basePermalink
            }
        }
    } else {
        $fileSlug = Get-SafeSlug $fileName
        
        $fileFolder = Split-Path $relativeContentPath -Parent
        $pathFromSection = $fileFolder.Replace($section, "").Trim('/')
        if ($pathFromSection) {
            $pathFromSectionSlugParts = $pathFromSection.Split('/') | ForEach-Object { Get-SafeSlug $_ }
            $hugoUrl = $basePermalink + ($pathFromSectionSlugParts -join '/') + "/" + $fileSlug + "/"
        } else {
            $hugoUrl = $basePermalink + $fileSlug + "/"
        }
    }
    
    $linkTargets = @()
    $linkTargets += $fileName
    $linkTargets += $fileName -replace '[-_]', ' '
    
    if ($fileName -eq "_index") {
        $folderName = (Split-Path $filePath -Parent).Split('\')[-1]
        if ($folderName) {
            $linkTargets += $folderName
            $linkTargets += $folderName -replace '[-_]', ' '
        }
    }
    
    $pathAsLinkTarget = $relativeContentPath -replace "\.md$", ""
    if ($pathAsLinkTarget -ne "/" -and $pathAsLinkTarget -ne "") {
        $linkTargets += $pathAsLinkTarget
        $linkTargets += $pathAsLinkTarget -replace '[-_]', ' '
    }
    
    foreach ($target in $linkTargets | Select-Object -Unique) {
        if ($target -ne "") {
            if (-not $linkMap.ContainsKey($target) -or $linkMap[$target].Length -lt $hugoUrl.Length) {
                $linkMap[$target] = $hugoUrl
            }
        }
    }
}

# 이미지 파일 매핑
$imagesFullPath = Join-Path $staticPath $imagesStaticSubPath

if (Test-Path $imagesFullPath) {
    Write-Host "이미지 파일 매핑 중..." -ForegroundColor Green
    
    $imageExtensions = @(".png", ".jpg", ".jpeg", ".gif", ".webp", ".svg", ".bmp")
    
    foreach ($extension in $imageExtensions) {
        Get-ChildItem -Path $imagesFullPath -Filter "*$extension" -Recurse -File | ForEach-Object {
            $imageFile = $_
            $imageName = $imageFile.Name
            
            $imageUrl = "$webImagePath/$imageName"
            
            $linkMap[$imageName] = $imageUrl
            
            $imageNameWithoutExt = [System.IO.Path]::GetFileNameWithoutExtension($imageName)
            if ($imageNameWithoutExt -ne $imageName) {
                $linkMap[$imageNameWithoutExt] = $imageUrl
            }
        }
    }
    
    Write-Host "이미지 파일 매핑 완료" -ForegroundColor Green
} else {
    Write-Host "이미지 폴더를 찾을 수 없습니다: $imagesFullPath" -ForegroundColor Yellow
}

Write-Host "생성된 URL 매핑 개수: $($linkMap.Count)" -ForegroundColor Green
Write-Host ""

# =================================================================
# 3단계: 프론트 매터 재작성 및 위키링크, 이미지 경로, PDF 경로 변환
# =================================================================

Write-Host "=== 3단계: 프론트 매터 재작성 및 위키링크, 이미지 경로, PDF 경로 변환 ===" -ForegroundColor Magenta

$totalConvertedLinks = 0
$totalConvertedImages = 0
$totalConvertedPDFs = 0
$totalFilesProcessed = 0
$totalFilesSkipped = 0
$problematicItems = @()

foreach ($section in $sections) {
    $sectionPath = Join-Path $contentPath $section
    
    if (Test-Path $sectionPath) {
        Write-Host "섹션 처리 중: $section" -ForegroundColor Green
        
        $allFolders = @($sectionPath) + (Get-ChildItem -Path $sectionPath -Directory -Recurse | Select-Object -ExpandProperty FullName)
        
        foreach ($folderPath in $allFolders) {
            $mdFiles = Get-ChildItem -Path $folderPath -Filter "*.md" -File | Sort-Object Name
            
            if ($mdFiles.Count -gt 0) {
                $relativePath = $folderPath.Replace($contentPath, "").TrimStart('\')
                Write-Host "  폴더: /$relativePath ($($mdFiles.Count)개 파일)" -ForegroundColor Cyan
                
                # 기존 파일들의 최대 weight 찾기
                $maxExistingWeight = 0
                
                foreach ($file in $mdFiles) {
                    if ($file.Name -eq "_index.md") {
                        continue
                    }
                    
                    $content = Get-Content -Path $file.FullName -Raw -Encoding UTF8
                    
                    if (Test-HasFrontMatter -content $content) {
                        if ($content -match '(?s)^---\s*\n([\s\S]*?)\n---') {
                            $existingFM = $matches[1]
                            if ($existingFM -match '(?m)^weight:\s*(\d+)') {
                                $existingWeight = [int]$matches[1]
                                if ($existingWeight -gt $maxExistingWeight) {
                                    $maxExistingWeight = $existingWeight
                                }
                            }
                        }
                    }
                }
                
                $weight = $maxExistingWeight + 1
                
                if ($maxExistingWeight -gt 0) {
                    Write-Host "    최대 기존 weight: $maxExistingWeight, 새 파일 시작 weight: $weight" -ForegroundColor DarkCyan
                }
                
                foreach ($file in $mdFiles) {
                    $content = Get-Content -Path $file.FullName -Raw -Encoding UTF8
                    
                    if (-not $fullRebuildMode -and (Test-HasFrontMatter -content $content)) {
                        Write-Host "    - 건너뜀 (기존 파일): $($file.Name)" -ForegroundColor DarkGray
                        $totalFilesSkipped++
                        continue
                    }
                    
                    Write-Host "    - 처리 중: $($file.Name)"
                    $totalFilesProcessed++
                    
                    $fileCreationTime = (Get-Item $file.FullName).CreationTime
                    
                    $cleanedBody = $content
                    
                    if ($content -match '(?s)^---\s*\n([\s\S]*?)\n---\s*\n([\s\S]*)$') {
                        $cleanedBody = $Matches[2]
                    }
                    
                    $cleanedBody = $cleanedBody -replace '(?s)<div\s+style="display:none">\s*<meta\s+property="article:published_time"[^>]*>\s*(<meta\s+property="article:modified_time"[^>]*>)?\s*</div>[\r\n]*', ''
                    $cleanedBody = $cleanedBody -replace '<meta property="article:(published|modified)_time"[^>]*>[\r\n]*', ''
                    $cleanedBody = $cleanedBody.Trim()
                    
                    $newTitle = ""
                    if ($file.Name -eq "_index.md") {
                        $newTitle = Split-Path $file.DirectoryName -Leaf
                    } else {
                        $newTitle = [System.IO.Path]::GetFileNameWithoutExtension($file.Name)
                    }
                    
                    $finalCreatedDate = $fileCreationTime.ToString("yyyy-MM-ddTHH:mm:sszzz")
                    
                    $newFMContent = @()
                    $newFMContent += "title: `"$newTitle`""
                    $newFMContent += "date: $finalCreatedDate"
                    $newFMContent += "type: docs"
                    $newFMContent += "sidebar:"
                    $newFMContent += "  open: false"
                    
                    if ($file.Name -ne "_index.md") {
                        $newFMContent += "weight: $weight"
                        Write-Host "      → weight: $weight 할당" -ForegroundColor DarkGreen
                    }
                    
                    # 위키링크 변환
                    $fileConvertedWikilinks = 0
                    $wikiLinkPattern = '\[\[([^\|\]]+)(?:\|([^\]]+))?\]\]'
                    
                    try {
                        $matches = [regex]::Matches($cleanedBody, $wikiLinkPattern)
                        
                        if ($matches.Count -gt 0) {
                            foreach ($match in $matches | Sort-Object -Descending { $_.Index }) {
                                $fullMatch = $match.Value
                                $target = $match.Groups[1].Value.Trim()
                                $alias = if ($match.Groups.Count -gt 2) { $match.Groups[2].Value.Trim() } else { "" }
                                
                                $linkText = if ($alias) { $alias } else { $target }
                                
                                $isImageWikilink = $target -match '\.(png|jpg|jpeg|gif|webp|svg|bmp)$'
                                
                                if ($target -match '%[^0-9A-Fa-f]|%$|%[0-9A-Fa-f]$') {
                                    $problematicItems += @{
                                        File = $file.FullName
                                        Item = $fullMatch
                                        Type = "Wikilink (Problematic Chars)"
                                        Target = $target
                                    }
                                    $cleanedBody = $cleanedBody.Remove($match.Index, $match.Length).Insert($match.Index, $linkText)
                                    continue
                                }
                                
                                $normalizedTarget = $target -replace '[-_]', ' '
                                $targetUrl = $null
                                
                                if ($linkMap.ContainsKey($target)) {
                                    $targetUrl = $linkMap[$target]
                                } elseif ($linkMap.ContainsKey($normalizedTarget)) {
                                    $targetUrl = $linkMap[$normalizedTarget]
                                } elseif ($target.Contains('/')) {
                                    $baseTargetName = Split-Path $target -Leaf
                                    if ($linkMap.ContainsKey($baseTargetName)) {
                                        $targetUrl = $linkMap[$baseTargetName]
                                    } elseif ($linkMap.ContainsKey($baseTargetName -replace '[-_]', ' ')) {
                                        $targetUrl = $linkMap[$baseTargetName -replace '[-_]', ' ']
                                    }
                                }
                                
                                if ($targetUrl) {
                                    if ($isImageWikilink) {
                                        $altText = if ($alias) { $alias } else { [System.IO.Path]::GetFileNameWithoutExtension($target) }
                                        $altText = $altText -replace '\s+', '-'
                                        $altText = $altText.Trim('-')
                                        $markdownLink = "![$altText]($targetUrl)"
                                        $fileConvertedWikilinks++
                                        $totalConvertedImages++
                                    } else {
                                        $markdownLink = "[$linkText]($targetUrl)"
                                        $fileConvertedWikilinks++
                                        $totalConvertedLinks++
                                    }
                                    $cleanedBody = $cleanedBody.Remove($match.Index, $match.Length).Insert($match.Index, $markdownLink)
                                } else {
                                    $problematicItems += @{
                                        File = $file.FullName
                                        Item = $fullMatch
                                        Type = "Wikilink (Unmapped)"
                                        Target = $target
                                    }
                                    $cleanedBody = $cleanedBody.Remove($match.Index, $match.Length).Insert($match.Index, $linkText)
                                }
                            }
                        }
                    }
                    catch {
                        Write-Host "      ⚠ 위키링크 처리 중 오류 발생: $($_.Exception.Message)" -ForegroundColor Red
                    }
                    
                    # 이미지 경로 변환
                    $fileConvertedSimpleImages = 0
                    
                    try {
                        $imageExtensions = @("png", "jpg", "jpeg", "gif", "webp", "svg", "bmp")
                        
                        foreach ($ext in $imageExtensions) {
                            $pattern = "![]("
                            
                            $startIndex = 0
                            while ($true) {
                                $index = $cleanedBody.IndexOf($pattern, $startIndex)
                                if ($index -eq -1) { break }
                                
                                $endIndex = $cleanedBody.IndexOf(")", $index + $pattern.Length)
                                if ($endIndex -eq -1) { break }
                                
                                $filename = $cleanedBody.Substring($index + $pattern.Length, $endIndex - $index - $pattern.Length)
                                
                                if ($filename -match "\.($($imageExtensions -join '|'))$" -and -not $filename.StartsWith("/")) {
                                    $oldImageTag = "$pattern$filename)"
                                    $newImageTag = "![]($webImagePath/$filename)"
                                    
                                    $cleanedBody = $cleanedBody.Replace($oldImageTag, $newImageTag)
                                    $fileConvertedSimpleImages++
                                    $totalConvertedImages++
                                    
                                    $startIndex = $index + $newImageTag.Length
                                } else {
                                    $startIndex = $endIndex + 1
                                }
                            }
                        }
                    }
                    catch {
                        Write-Host "      ⚠ 이미지 처리 중 오류 발생: $($_.Exception.Message)" -ForegroundColor Red
                    }
                    
                    # PDF 경로 변환 (로컬 경로 → GitHub 경로)
                    $fileConvertedPDFs = 0
                    
                    try {
                        # PDF 위키링크 패턴: [[파일명.pdf]]
                        $pdfWikilinkPattern = '\[\[([^\]]+\.pdf)\]\]'
                        $pdfMatches = [regex]::Matches($cleanedBody, $pdfWikilinkPattern)
                        
                        if ($pdfMatches.Count -gt 0) {
                            foreach ($pdfMatch in $pdfMatches | Sort-Object -Descending { $_.Index }) {
                                $pdfFileName = $pdfMatch.Groups[1].Value.Trim()
                                $pdfFileName = Split-Path $pdfFileName -Leaf  # 경로가 포함되어 있을 경우 파일명만 추출
                                
                                # URL 인코딩 (공백 등 특수문자 처리)
                                $encodedFileName = [System.Web.HttpUtility]::UrlEncode($pdfFileName)
                                $pdfGitHubUrl = "$pdfGitHubBaseUrl/$encodedFileName"
                                
                                $pdfMarkdownLink = "[$pdfFileName]($pdfGitHubUrl)"
                                $cleanedBody = $cleanedBody.Remove($pdfMatch.Index, $pdfMatch.Length).Insert($pdfMatch.Index, $pdfMarkdownLink)
                                
                                $fileConvertedPDFs++
                                $totalConvertedPDFs++
                            }
                        }
                        
                        # 일반 Markdown 링크 중 로컬 PDF 경로 변환: [텍스트](로컬경로/파일.pdf)
                        $localPdfLinkPattern = '\[([^\]]+)\]\((?!http)([^\)]+\.pdf)\)'
                        $localPdfMatches = [regex]::Matches($cleanedBody, $localPdfLinkPattern)
                        
                        if ($localPdfMatches.Count -gt 0) {
                            foreach ($localPdfMatch in $localPdfMatches | Sort-Object -Descending { $_.Index }) {
                                $linkText = $localPdfMatch.Groups[1].Value
                                $localPdfPath = $localPdfMatch.Groups[2].Value.Trim()
                                $pdfFileName = Split-Path $localPdfPath -Leaf
                                
                                $encodedFileName = [System.Web.HttpUtility]::UrlEncode($pdfFileName)
                                $pdfGitHubUrl = "$pdfGitHubBaseUrl/$encodedFileName"
                                
                                $pdfMarkdownLink = "[$linkText]($pdfGitHubUrl)"
                                $cleanedBody = $cleanedBody.Remove($localPdfMatch.Index, $localPdfMatch.Length).Insert($localPdfMatch.Index, $pdfMarkdownLink)
                                
                                $fileConvertedPDFs++
                                $totalConvertedPDFs++
                            }
                        }
                    }
                    catch {
                        Write-Host "      ⚠ PDF 경로 처리 중 오류 발생: $($_.Exception.Message)" -ForegroundColor Red
                    }
                    
                    if ($fileConvertedWikilinks -gt 0) {
                        Write-Host "      ✓ $($fileConvertedWikilinks)개의 위키링크 변환 완료." -ForegroundColor Green
                    }
                    if ($fileConvertedSimpleImages -gt 0) {
                        Write-Host "      ✓ $($fileConvertedSimpleImages)개의 '![](파일명)' 변환 완료." -ForegroundColor Green
                    }
                    if ($fileConvertedPDFs -gt 0) {
                        Write-Host "      ✓ $($fileConvertedPDFs)개의 PDF 경로 변환 완료." -ForegroundColor Green
                    }
                    
                    $finalFrontMatterBlock = "---`n" + ($newFMContent -join "`n") + "`n---"
                    $finalContent = $finalFrontMatterBlock + "`n" + $cleanedBody
                    
                    Set-Content -Path $file.FullName -Value $finalContent -Encoding UTF8
                    
                    Write-Host "        ✓ 완료" -ForegroundColor Green
                    
                    $weight++
                }
            }
        }
    } else {
        Write-Host "경로를 찾을 수 없습니다: $sectionPath - 스킵합니다." -ForegroundColor Yellow
    }
}

Write-Host ""
Write-Host "  총 $totalFilesSkipped 개의 파일 건너뜀 (기존 파일)" -ForegroundColor Cyan
Write-Host ""

if ($problematicItems.Count -gt 0) {
    Write-Host "⚠ 문제성 항목 (매핑 실패 또는 잘못된 형식) 목록:" -ForegroundColor Yellow
    foreach ($item in $problematicItems) {
        Write-Host "  - 파일: $($item.File)" -ForegroundColor Yellow
        Write-Host "    유형: $($item.Type)" -ForegroundColor Yellow
        Write-Host "    항목: $($item.Item)" -ForegroundColor Yellow
        if ($item.Target) { Write-Host "    타겟: $($item.Target)" -ForegroundColor Yellow }
    }
    Write-Host ""
}

Write-Host "========================================" -ForegroundColor Magenta
Write-Host "   모든 자동화 작업 완료!" -ForegroundColor Magenta
Write-Host "   ✓ 누락된 _index.md 파일 자동 생성/업데이트" -ForegroundColor Magenta
if ($fullRebuildMode) {
    Write-Host "   ⚠️  전체 재정리 모드: 모든 파일 프론트 매터 재작성" -ForegroundColor Red
} else {
    Write-Host "   ✅ 신규 파일만 처리 모드: 기존 파일 $totalFilesSkipped 개 건너뜀 (lastmod 보존)" -ForegroundColor Green
}
Write-Host "   ✓ Permalinks 기반 URL 매핑 적용" -ForegroundColor Magenta
Write-Host "   ✓ 위키링크 ([[]]) 변환 지원" -ForegroundColor Magenta
Write-Host "   ✓ '![](파일명)' 형태 이미지 경로 변환 지원" -ForegroundColor Magenta
Write-Host "   ✓ PDF 파일 경로를 GitHub 경로로 변환" -ForegroundColor Magenta
Write-Host "   ✓ 총 $($totalConvertedLinks)개의 일반 위키링크 변환" -ForegroundColor Magenta
Write-Host "   ✓ 총 $($totalConvertedImages)개의 이미지 링크 변환" -ForegroundColor Magenta
Write-Host "   ✓ 총 $($totalConvertedPDFs)개의 PDF 링크 변환" -ForegroundColor Magenta
Write-Host "   ✓ 총 $($totalFilesProcessed)개의 파일 프론트 매터 처리" -ForegroundColor Magenta
Write-Host "========================================" -ForegroundColor Magenta
Write-Host ""
Write-Host "이제 다음 단계를 진행하세요:" -ForegroundColor Cyan
Write-Host "1. hugo server -D 로 로컬 테스트" -ForegroundColor White
Write-Host "2. 문제 없으면 git add ." -ForegroundColor White
Write-Host "3. git commit -m 'Complete automation: preserve existing files, convert PDF paths to GitHub'" -ForegroundColor White
Write-Host "4. git push origin main" -ForegroundColor White
Write-Host "========================================" -ForegroundColor Magenta
