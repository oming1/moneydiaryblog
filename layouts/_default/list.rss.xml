<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>{{ if eq .Title .Site.Title }}{{ .Site.Title }}{{ else }}{{ .Site.Title }} – {{ .Title }}{{ end }}</title>
    <link>{{ .Permalink }}</link>
    <description>Recent content {{ if ne  .Title  .Site.Title }}{{ with .Title }}in {{.}} {{ end }}{{ end }}on {{ .Site.Title }}</description>
    <generator>Hugo -- gohugo.io</generator>{{ with .Site.LanguageCode }}
    <language>{{.}}</language>{{end}}{{ with .Site.Author.email }}
    <managingEditor>{{.}}{{ with $.Site.Author.name }} ({{.}}){{end}}</managingEditor>{{end}}{{ with .Site.Author.email }}
    <webMaster>{{.}}{{ with $.Site.Author.name }} ({{.}}){{end}}</webMaster>{{end}}{{ with .Site.Copyright }}
    <copyright>{{.}}</copyright>{{end}}{{ if not .Date.IsZero }}
    <lastBuildDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</lastBuildDate>{{ end }}
    {{ with .OutputFormats.Get "RSS" }}
	  {{ printf "<atom:link href=%q rel=\"self\" type=%q />" .Permalink .MediaType | safeHTML }}
    {{ end }}
    
    {{/* --- 수정된 부분: RSS에 포함할 페이지 필터링 로직 --- */}}
    {{ $rssPages := $.Site.RegularPages }}
    {{ if $.IsHome }}
      {{/* 홈페이지 RSS일 경우, 모든 type:docs 페이지 중 최신 50개 */}}
      {{ $rssPages = first 50 (where $.Site.RegularPages "Type" "docs") }}
    {{ else if $.IsSection }}
      {{/* 섹션 페이지 RSS일 경우, 해당 섹션 내 type:docs 페이지 중 최신 50개 */}}
      {{ $rssPages = first 50 (where $.Page.RegularPages "Type" "docs") }}
    {{ else if $.IsTerm }}
      {{/* Taxonomy (태그/카테고리) 페이지 RSS일 경우, 해당 태그/카테고리 내 type:docs 페이지 중 최신 50개 */}}
      {{ $rssPages = first 50 (where $.Page.RegularPages "Type" "docs") }}
    {{ end }}
    {{/* --- 수정 끝 --- */}}

    {{ range $rssPages.ByDate.Reverse }}
    <item>
      <title>{{ .Title }}</title>
      <link>{{ .Permalink }}</link>
      <pubDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</pubDate>
      {{ with .Site.Author.email }}<author>{{.}}{{ with $.Site.Author.name }} ({{.}}){{end}}</author>{{end}}
      <guid>{{ .Permalink }}</guid>
      <description>
        {{/* 피쳐드 이미지 추출 및 HTML에 삽입 (Resources에 있는 이미지 활용) */}}
        {{ $img := (.Resources.ByType "image").GetMatch "*featured*" }}
        {{ with $img }}
        {{ $img := .Resize "640x" }}
        {{ printf "<![CDATA[<img src=\"%s\" width=\"%d\" height=\"%d\"/>]]>" $img.Permalink $img.Width $img.Height | safeHTML }}
        {{ end }}
        {{/* 본문 내용 전체를 HTML로 변환하여 삽입 */}}
        {{ .Content | html }}
      </description>
    </item>
    {{ end }}
  </channel>
</rss>
